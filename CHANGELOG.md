# 시계열 데이터 전처리 프로그램 - 변경 이력

## Unreleased
### 웹 UI
- 파일 로드 진행률 바: 파일 읽기→엑셀 파싱→CSV 변환→컬럼 분석→화면 구성 단계별 표시 및 파일 크기(MB) 실시간 표기
- 전처리 실행 진행률 바: 차트/통계 갱신·Validation 생성·Simulation/Excel 저장 구간까지 연장 (기존 70%→100% 구간 세분화)
- 전처리 실행 중 버튼 비활성화, 모든 저장 완료 후 복구

### 수정
- Windows 전처리 스레드가 GUI 위젯을 직접 읽지 않도록 실행 전 설정 스냅샷을 전달하도록 정리
- Windows 전처리 오류 로그에서 예외 메시지가 누락될 수 있는 lambda 캡처 문제 수정
- Mac 전처리 스레드가 GUI 위젯을 직접 읽지 않도록 설정 스냅샷 기반으로 실행
- Mac Validation 버튼을 메인 화면에 추가하고, 이상값 처리는 실제 정책과 동일하게 `행 전체 삭제`만 노출
- Mac 미리보기 테이블에서 결측값이 `nan` 문자열로 보이지 않도록 수정
- Mac 전처리 취소 시 버튼 상태가 복구되도록 완료 신호를 보냄

### 웹 UI
- 메인 웹 화면의 `사용 순서`를 8단계 인터랙티브 매뉴얼로 확장해 각 단계별 하이라이트, 왜 필요한지, 기존 절차 대비 개선점, 자동화 포인트, 저장 파일 구성을 안내
- 인터랙티브 매뉴얼 후반을 실행·자동 저장·로그 재확인까지 분리해 저장 파일 확인 흐름을 명확화
- 일반 `i` 도움말을 화면 경계에 잘리지 않는 floating tooltip으로 변경
- 이상값/선제거 리치 도움말을 면적 기반 그래프와 일관된 레이아웃으로 정리
- 실행 버튼 아래에 `_prepro_with_valid.xlsx`와 `Simulation_Data_*.xlsx` 저장 파일 구성 안내를 표시

### 문서
- README/MANUAL/웹 계획 문서를 현재 코드 상태에 맞게 동기화
- 구형 `.xls` 경로를 제거하고 `.xlsx`/CSV 중심 지원 범위로 문서를 정리
- 데스크톱 UI에서 별도 시뮬레이션 생성 메뉴가 노출되지 않는다는 현재 상태를 명시
- `test_time_norm.py`의 pandas 초 단위 빈도 표기를 `61s`로 바꿔 FutureWarning 제거
- 포털 서버형/로컬 패키징형을 모두 고려한 OPC helper tag 조건 설계 문서 추가
- 독립 실행용 `preprocessing_portal` 태그 검색/backend 골격과 발전소 prefix별 OPC tag index 추가
- `Preprocessing.html`에 OPC 보조 태그 검색/선택/helper 컬럼 부착 UI와 API hook 연결
- OPC helper 조회 기준을 업로드 XLSX의 좌측 첫 시간 컬럼으로 고정하고 시간 정규화/재정렬 기본 체크 상태를 명시
- 웹 필터 `!=`, 필터 도움말, 이상값 대상 기본 해제/Simulation 자동 동기화, 2분 배수 시작시간 보정, 빈 헤더 열 제거 동작을 문서화
- Finder 전체 압축 대신 사용할 VDI 테스트용 zip 생성 스크립트 추가

## v1.7.0 (2026-03-12)
### 추가
- Validation 데이터 자동 생성 기능
- 전처리본 / Validation 포함본 / Validation 전용본 3종 자동 저장
- 선택 태그 기준 제거행 기반 / sigma 기반 synthetic validation 생성
- Validation 설정 프리셋 저장/복원

### 변경
- 이상값 처리 재실행 시 제거행 상태 초기화
- 분석 메뉴를 `Validation 데이터 생성` 기준으로 정리

## v1.6.0 (2024-12-24)
### 추가
- 시뮬레이션 데이터 생성 기능 추가
- ML 모델 테스트용 정상 → 비정상 전환 데이터 생성
- 전환 구간 Gradient 보간

## v1.5.2 (2024-12-23)
### 변경
- 파일명에 버전 표시 (DataPreprocessor_v1.5.2.exe)
- 윈도우 타이틀에 버전 표시
- version.py 추가 (중앙집중식 버전 관리)

## v1.5.1 (2024-12-23)
### 추가
- 다중 컬럼 트렌드 차트 (Ctrl+클릭으로 최대 5개)
- 컬럼별 색상 구분 (5가지 팔레트)
- 인터랙티브 호버 커서 (컬럼명 + 값 표시)
- 정규화 옵션 (스케일 다른 컬럼 비교)

## v1.5.0 (2024-12-23)
### 추가
- 분석 → 트렌드 차트 메뉴 (Ctrl+T)
- 자동 스케일 (20% 여유)
- 평균선 표시
- 통계 정보 (최소/최대/평균/표준편차)

## v1.4.0 (2024-12-23)
### 추가
- Mac 버전 (PyQt5 기반) gui_app_mac.py
- 핵심 로직 공유 (Windows/Mac 동일)

## v1.3.2 (2024-12-23)
### 추가
- 사용자 매뉴얼 (도움말 → 사용자 매뉴얼, F1)
- MANUAL.md 생성 (9개 섹션)
- 내장 폴백 매뉴얼

## v1.3.1 (2024-12-23)
### 추가
- 프로그램 정보 다이얼로그 (About)
- 개발자 정보 외부 파일 지원
- developer_info.json 템플릿

## v1.3.0 (2024-12-23)
### 추가
- 프리셋 저장/불러오기
- 프리셋 관리/내보내기/가져오기
- 파일+프리셋 한번에 열기 (원클릭 전처리)

## v1.2.0 (2024-12-23)
### 추가
- 시간 정규화 기능 (2분 간격 스냅)
- Excel 자동채우기 시간 밀림 보정

## v1.1.0 (2024-12-23)
### 추가
- 도움말 툴팁
- 동적 컬럼 표시 (최대 30개)
- Date 형식 보존
- 시간 재정렬 기능

## v1.0.0 (2024-12-23)
### 최초 릴리스
- Excel/CSV 파일 로드
- 다중 조건 필터링 (AND, range 지원)
- 이상값 처리 (2σ, 2.5σ, 3σ, IQR)
- 정규화 (Z-Score, Min-Max)
- 결과 저장 (원본 형식 보존)
- 진행률 표시 (대용량 데이터 지원)
