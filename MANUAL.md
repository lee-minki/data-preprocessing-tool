# σ 조기경보 시계열 데이터 전처리프로그램 사용자 매뉴얼

**Version 1.7.0**
적용 대상: Windows 실행 파일 (`DataPreprocessor_v1.7.0.exe`) / `gui_app.py` / `gui_app_mac.py`

## 1. 프로그램 개요

이 프로그램은 Excel/CSV 형식의 시계열 데이터를 불러와 다음 작업을 빠르게 수행하기 위한 데스크톱 도구입니다.

- 숫자 컬럼 기준 다중 필터링
- 이상값 탐지 및 처리
- 시간 보정 및 시간 재정렬
- 프리셋 저장/불러오기
- 트렌드 차트 확인
- Validation 데이터 자동 생성
- 시뮬레이션 데이터 생성

## 2. 지원 환경과 파일 형식

### 지원 플랫폼
- Windows: `DataPreprocessor.exe` 또는 `python gui_app.py`
- Mac: `python gui_app_mac.py`

### 지원 파일 형식
- Excel: `.xlsx`, `.xls`
- CSV: `.csv`

### CSV 인코딩
CSV는 아래 순서로 자동 읽기를 시도합니다.

- UTF-8
- CP949
- EUC-KR

## 3. 시작 전 알아둘 점

- 날짜 컬럼은 `Date`, `Time`, `Datetime`, `날짜`, `시간`, `timestamp` 같은 이름을 기준으로 자동 감지합니다.
- 필터/이상값 처리 대상은 숫자형 컬럼만 포함됩니다.
- 숫자 컬럼은 최대 30개까지 UI에 표시됩니다.
- `Unnamed`로 시작하는 빈 컬럼은 자동 제거됩니다.

## 4. 가장 빠른 사용 방법

1. 프로그램 실행
2. `파일 불러오기`로 Excel/CSV 선택
3. 필요하면 필터 추가
4. 필요하면 이상값 처리/시간 처리 옵션 선택
5. `🚀 전처리 실행`
6. 하단 결과 로그 확인
7. `💾 결과 저장`

## 5. 화면 구성

### 5.1 파일 선택
- `파일 불러오기`: 데이터 파일 선택
- 파일명 표시: 현재 연 파일 이름 표시
- 데이터 정보: 행/열 수 표시
- 미리보기 테이블: 상위 일부 데이터를 바로 확인

### 5.2 필터 조건
- `+ 필터 추가`로 조건을 여러 개 넣을 수 있습니다.
- 여러 조건은 모두 `AND`로 결합됩니다.
- 각 조건은 `컬럼`, `연산자`, `값` 또는 `범위`로 구성됩니다.

### 5.3 이상값 처리
- 기본값은 `적용` 상태입니다.
- 방법: `2σ`, `2.5σ`, `3σ`, `IQR`
- 처리 결과: 이상값이 포함된 행은 전체 삭제됩니다.

### 5.4 시간 처리
- `시간 정규화`: 틀어진 시간을 가장 가까운 간격으로 보정
- `시간 재정렬`: 지정한 시작 시각부터 일정 간격으로 다시 생성
- `간격(분)` 기본값은 `2`입니다.
- 시간 정규화 체크박스 문구는 2분 기준으로 보이지만, 실제 계산에는 아래 `간격(분)` 입력값이 사용됩니다.

### 5.6 진행 상황 / 결과
- 진행률 바와 상태 문구를 표시합니다.
- 처리 결과 로그에 필터링/이상값/시간 처리 결과가 순서대로 남습니다.

## 6. 실제 작업 순서

전처리 실행 시 내부 순서는 아래와 같습니다.

1. 필터링
2. 이상값 처리
3. 시간 정규화
4. 시간 재정렬

필터가 없으면 원본 전체 데이터를 그대로 사용합니다.

## 7. 기능별 사용법

### 7.1 파일 불러오기

1. `파일 > 열기...` 또는 `파일 불러오기` 클릭
2. Excel/CSV 파일 선택
3. 상단에 파일명, 우측에 행/열 수가 표시되는지 확인
4. 미리보기 테이블과 감지된 숫자 컬럼 정보를 확인

대용량 파일은 로딩에 시간이 걸릴 수 있으며 진행 상태가 표시됩니다.

### 7.2 필터링

#### 지원 연산자
- `>=`
- `<=`
- `>`
- `<`
- `=`
- `!=`
- `range`

#### 예시
- `AMBIENT_TEMP >= 15`
- `FAN_CURRENT range 30 ~ 50`
- `GEARBOX_OIL_TEMP < 85` AND `FAN_CURRENT >= 40`

#### 참고
- `range`는 최소값 또는 최대값 한쪽만 입력해도 동작합니다.
- 숫자가 아닌 값은 필터 조건으로 넣을 수 없습니다.

### 7.3 이상값 처리

#### 방법 선택 기준

| 방법 | 설명 | 추천 상황 |
|------|------|-----------|
| 2σ | 엄격하게 제거 | 이상값을 강하게 걷어낼 때 |
| 2.5σ | 기본 추천 | 일반적인 현업 전처리 |
| 3σ | 느슨하게 제거 | 극단값만 제거하고 싶을 때 |
| IQR | 사분위수 기반 | 비대칭 분포일 때 |

#### 주의
- 이상값이 포함된 행은 항상 전체 삭제됩니다.
- 제거된 행은 Validation 데이터 생성 기능의 재료로 저장됩니다.

### 7.4 시뮬레이션 데이터 생성

목적:
- 전처리 중 제거된 이상값을 활용해 ML 테스트용 시계열 데이터를 생성
- 선택한 컬럼만 정상 → 전환 → 비정상 흐름으로 변화

생성 조건:
- 먼저 전처리를 실행해야 합니다.
- 제거된 이상값 행이 있어야 합니다.
- 대상 컬럼을 1개 이상 선택해야 합니다.

출력:
- 기본 파일명: `Simulation_Data_YYYYMMDD_HHMMSS.xlsx`
- 시트명: `Simulation`

구성:
- 정상 구간
- 점진 전환 구간
- 비정상 구간

### 7.5 시간 정규화

엑셀 자동채우기 등으로 시간이 조금씩 밀린 경우 보정합니다.

예시:

| 원본 | 보정 후 |
|------|---------|
| `00:01:00` | `00:00:00` |
| `00:02:01` | `00:02:00` |
| `00:05:59` | `00:06:00` |

추천 상황:
- 원래 2분 또는 5분 간격 데이터인데 초 단위가 어긋난 경우

### 7.6 시간 재정렬

필터링 이후 남은 데이터의 시간축을 새로 만듭니다.

입력값:
- 시작 시간: 예) `2025-01-01 00:00:00`
- 간격(분): 예) `2`

결과:
- 첫 행부터 시작 시간 적용
- 이후 행은 지정한 분 간격으로 증가

### 7.7 Validation 데이터 생성

위치:
- 메인 화면 `🧪 Validation 데이터 생성` 버튼

목적:
- 전처리 결과를 기준으로 validation 전용 데이터를 자동 생성
- 선택한 태그에만 이상 패턴을 넣은 파일 생성

입력값:
- Validation 비율
- 이상값 발생 컬럼(다중 선택)
- 내부 구간 비율 4개
  - 정상1
  - 제거행 기반 이상
  - 정상2
  - Sigma 기반 이상
- Sigma 시작/끝 배수

생성 조건:
- 전처리 과정에서 제거된 행이 있어야 합니다.
- 제거된 이상값이 없으면 생성할 수 없습니다.

출력:
- 원본 파일 폴더에 자동 저장
- `원본이름_prepro.xlsx`
- `원본이름_prepro_with_valid.xlsx`
- `원본이름_valid.xlsx`

### 7.8 결과 저장

1. `💾 결과 저장` 클릭
2. 저장할 경로와 형식 선택
3. CSV 또는 Excel로 저장

#### 저장 규칙
- 현재 파일이 있으면 기본 파일명은 `원본이름_processed_YYYYMMDD_HHMMSS`
- 날짜 컬럼은 `YYYY-MM-DD HH:MM:SS` 형식으로 저장
- CSV는 `utf-8-sig`로 저장

## 8. 프리셋 기능

반복 작업이 많을 때 프리셋을 사용하면 빠릅니다.

### 8.1 프리셋 저장

1. 필터, 이상값, 시간 처리 설정 완료
2. `프리셋 > 프리셋 저장...`
3. 이름과 설명 입력

### 8.2 프리셋 불러오기

1. `프리셋 > 프리셋 불러오기...`
2. 프리셋 선택
3. 필요하면 `불러온 후 자동으로 전처리 실행` 체크

### 8.3 파일 + 프리셋 한번에 열기

반복 작업용 원클릭 모드입니다.

1. `프리셋 > 파일+프리셋 한번에 열기...`
2. 프리셋 선택
3. 데이터 파일 선택
4. 자동 전처리 실행
5. 옵션 체크 시 자동 저장

### 8.4 프리셋 내보내기 / 가져오기
- 내보내기: 프리셋을 JSON 파일로 저장
- 가져오기: 받은 JSON 프리셋을 로컬에 등록

### 8.5 프리셋 저장 위치
- 기본 경로: `~/.data_preprocessor/presets/`

## 9. 분석 기능

### 9.1 트렌드 차트

메뉴: `분석 > 📊 트렌드 차트...`

가능한 작업:
- 최대 5개 컬럼 동시 비교
- `Ctrl+클릭` 다중 선택
- 자동 스케일
- 평균선 표시
- 컬럼별 최소/최대/평균/표준편차 확인

사용 전제:
- 먼저 전처리를 실행해 `processed_df`가 있어야 합니다.

### 9.2 Validation 데이터 생성
- 자세한 절차는 `7.8 Validation 데이터 생성` 참고

### 9.3 시뮬레이션 데이터 생성
- 자세한 절차는 `7.4 시뮬레이션 데이터 생성` 참고

## 10. 메뉴와 단축키

### 공통
- `Ctrl+O`: 파일 열기
- `Ctrl+S`: 결과 저장
- `Ctrl+P`: 프리셋 저장
- `Ctrl+T`: 트렌드 차트
- `F1`: 사용자 매뉴얼

### 플랫폼별 추가
- Mac 버전:
  - `Ctrl+Q`: 종료
  - `Ctrl+Shift+S`: Validation 데이터 생성

## 11. 권장 사용 시나리오

### 시나리오 A: 일반 전처리
1. 파일 불러오기
2. 필요한 필터 추가
3. 이상값 처리 방식 선택
4. 전처리 실행
5. 결과 저장

### 시나리오 B: 시간축 보정
1. 파일 불러오기
2. `시간 정규화` 체크
3. 필요 시 `시간 재정렬`도 함께 체크
4. 시작 시간과 간격 설정
5. 전처리 실행 후 저장

### 시나리오 C: 반복 배치 작업
1. 한 번 수동으로 조건 세팅
2. 프리셋 저장
3. 다음부터 `파일+프리셋 한번에 열기` 사용

### 시나리오 D: Validation 데이터 만들기
1. 전처리 실행해서 필터링/이상값 제거 수행
2. 메인 화면의 `Validation 데이터 생성` 버튼 클릭
3. 컬럼, validation 비율, 구간 비율 선택
4. 원본 폴더에 생성된 3개 Excel 파일 확인

## 12. 자주 발생하는 문제

### 파일이 열리지 않을 때
- 확장자가 `.xlsx`, `.xls`, `.csv`인지 확인
- CSV 인코딩이 깨졌다면 UTF-8/CP949/EUC-KR 여부 확인
- 파일이 다른 프로그램에서 잠겨 있지 않은지 확인

### 날짜 컬럼이 인식되지 않을 때
- 컬럼명에 `Date`, `Time`, `Datetime`, `날짜`, `시간`, `timestamp` 중 하나가 포함되는지 확인
- 날짜 형식이 너무 불규칙하면 먼저 원본 파일에서 정리 후 다시 불러오기

### 필터 컬럼이 안 보일 때
- 숫자형 컬럼인지 확인
- 날짜 컬럼이나 문자 컬럼은 필터 목록에 나타나지 않음

### Validation 데이터 생성이 안 될 때
- 전처리를 먼저 실행했는지 확인
- 필터링 또는 이상값 처리로 실제 제거된 행이 있는지 확인
- 선택한 태그가 실제 숫자형 컬럼인지 확인
- 내부 구간 비율 합계가 100인지 확인

### 저장 형식이 다르게 보일 때
- Excel과 CSV는 표시 방식이 다를 수 있음
- 실제 저장 시 날짜는 `YYYY-MM-DD HH:MM:SS` 형식으로 맞춰집니다

## 13. 수동 검증 절차

아래 순서로 직접 재현하면 기본 기능을 빠르게 확인할 수 있습니다.

1. `sample_data.csv` 불러오기
2. 필터 추가: `AMBIENT_TEMP >= 15`
3. 이상값 처리: `2.5σ`, `행 전체 삭제`
4. 필요 시 시간 처리 체크
5. `🚀 전처리 실행`
6. 결과 로그에서 행 수 감소 여부 확인
7. `💾 결과 저장`으로 새 파일 생성 확인

## 14. 문의 / 배포 정보

- GitHub: https://github.com/lee-minki/data-preprocessing-tool
- Issues: https://github.com/lee-minki/data-preprocessing-tool/issues
