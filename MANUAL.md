# 시계열 데이터 전처리 프로그램 - 사용자 매뉴얼

**Version 1.3.1** | 최종 업데이트: 2024-12-22

---

## 목차

1. [프로그램 소개](#1-프로그램-소개)
2. [설치 방법](#2-설치-방법)
3. [기본 사용법](#3-기본-사용법)
4. [필터링 기능](#4-필터링-기능)
5. [이상값 처리](#5-이상값-처리)
6. [시간 처리](#6-시간-처리)
7. [정규화](#7-정규화)
8. [프리셋 기능](#8-프리셋-기능)
9. [자주 묻는 질문](#9-자주-묻는-질문)

---

## 1. 프로그램 소개

시계열 데이터를 전처리하는 Windows 데스크톱 프로그램입니다.

### 주요 기능
- 📁 Excel/CSV 파일 로드 (최대 20만행+)
- 🔧 다중 조건 필터링 (AND 결합)
- 📊 이상값 탐지 및 제거
- 🕐 시간 정규화 및 재정렬
- 📈 데이터 정규화 (Z-Score, Min-Max)
- 💾 프리셋 저장/불러오기

### 지원 파일 형식
| 형식 | 확장자 |
|------|--------|
| Excel | .xlsx, .xls |
| CSV | .csv |

---

## 2. 설치 방법

### 방법 1: 실행 파일 (권장)
1. [GitHub Releases](https://github.com/lee-minki/data-preprocessing-tool/releases) 접속
2. 최신 버전의 `DataPreprocessor.exe` 다운로드
3. 다운로드한 파일 실행

> ⚠️ Windows Defender 경고 시: "추가 정보" → "실행" 클릭

### 방법 2: Python 소스 코드
```bash
# 저장소 클론
git clone https://github.com/lee-minki/data-preprocessing-tool.git
cd data-preprocessing-tool

# 의존성 설치
pip install -r requirements.txt

# 실행
python gui_app.py
```

---

## 3. 기본 사용법

### 단계별 가이드

1. **파일 불러오기**
   - `파일 → 열기` 또는 `Ctrl+O`
   - Excel/CSV 파일 선택

2. **필터 조건 설정**
   - `+ 필터 추가` 버튼 클릭
   - 컬럼, 연산자, 값 설정

3. **이상값 처리 설정**
   - 방법 선택 (2.5σ 권장)
   - 처리 방식 선택 (행 삭제 권장)

4. **전처리 실행**
   - `🚀 전처리 실행` 버튼 클릭
   - 진행률 확인

5. **결과 저장**
   - `💾 결과 저장` 버튼 클릭
   - 저장 위치 선택

---

## 4. 필터링 기능

여러 조건을 AND로 결합하여 데이터를 필터링합니다.

### 연산자 종류

| 연산자 | 설명 | 예시 |
|--------|------|------|
| `>=` | 이상 | TEMP >= 15 |
| `<=` | 이하 | TEMP <= 30 |
| `>` | 초과 | CURRENT > 40 |
| `<` | 미만 | CURRENT < 60 |
| `=` | 같음 | STATUS = 1 |
| `!=` | 다름 | STATUS != 0 |
| `range` | 범위 | CURRENT 30~50 |

### 사용 예시
- 대기온도 15°C 이상 AND 운전전류 30~50A
- 기어박스 온도 50~80°C AND 팬 전류 40A 이상

---

## 5. 이상값 처리

### 방법 비교

| 방법 | 포함 비율 | 적합한 상황 |
|------|----------|------------|
| 2σ | 95.4% | 엄격한 필터링 |
| **2.5σ** | **98.8%** | **일반적 권장** |
| 3σ | 99.7% | 느슨한 필터링 |
| IQR | 가변 | 비대칭 분포 |

### 처리 방식

| 방식 | 설명 | 권장 |
|------|------|------|
| **행 전체 삭제** | 이상값 포함 행 삭제 | ✅ 기본값 |
| NaN으로 변경 | 해당 값만 NaN | 행 유지 필요 시 |

---

## 6. 시간 처리

### 시간 정규화 (Time Snap)

엑셀 자동채우기로 인한 시간 밀림을 보정합니다.

| 원본 시간 | 정규화 후 |
|-----------|----------|
| 00:01:00 | 00:00:00 |
| 00:02:01 | 00:02:00 |
| 00:05:59 | 00:06:00 |

**사용 시점**: 시간이 정확히 2분 간격이 아닐 때

### 시간 재정렬

필터링된 데이터를 새로운 시작 시간부터 재배열합니다.

- **시작 시간**: 새로 시작할 시간 (예: 2024-01-01 00:00:00)
- **간격**: 시간 간격 (분, 기본값: 2)

---

## 7. 정규화

머신러닝 입력을 위한 데이터 스케일링입니다.

### Z-Score 정규화
```
정규화된 값 = (원본 값 - 평균) / 표준편차
```
- 결과: 평균=0, 표준편차=1
- 용도: 데이터 비교, 일반적인 ML

### Min-Max 정규화
```
정규화된 값 = (원본 값 - 최소) / (최대 - 최소)
```
- 결과: 0~1 범위
- 용도: 신경망 입력

---

## 8. 프리셋 기능

반복 작업을 줄이기 위한 설정 저장/불러오기 기능입니다.

### 프리셋 저장
1. 필터, 이상값, 시간 처리 설정 완료
2. `프리셋 → 프리셋 저장` (Ctrl+P)
3. 이름과 설명 입력

### 프리셋 불러오기
1. `프리셋 → 프리셋 불러오기`
2. 목록에서 선택
3. "불러온 후 자동 전처리" 옵션 가능

### 원클릭 전처리
1. `프리셋 → 파일+프리셋 한번에 열기`
2. 프리셋 선택
3. 파일 선택 → 자동 전처리 실행

### 프리셋 배포
- `프리셋 → 프리셋 내보내기`: JSON 파일로 저장
- `프리셋 → 프리셋 가져오기`: JSON 파일에서 로드

엔지니어가 프리셋을 만들어 배포하면 작업자가 손쉽게 사용 가능!

---

## 9. 자주 묻는 질문

### Q: 지원하는 최대 데이터 크기는?
A: 20만 행 이상도 처리 가능합니다. 진행률 표시로 상태 확인이 가능합니다.

### Q: Date 열 형식이 깨지는 경우?
A: 프로그램이 자동으로 날짜 형식을 보존합니다. 저장 시 `YYYY-MM-DD HH:MM:SS` 형식으로 저장됩니다.

### Q: 프리셋 저장 위치는?
A: `~/.data_preprocessor/presets/` 폴더에 JSON 형식으로 저장됩니다.

### Q: 실행 파일이 작동하지 않는 경우?
A: Windows Defender나 백신에서 차단될 수 있습니다. 예외 등록 후 다시 실행하세요.

---

## 문의

- **GitHub**: https://github.com/lee-minki/data-preprocessing-tool
- **Issues**: https://github.com/lee-minki/data-preprocessing-tool/issues

---

*© 2024 시계열 데이터 전처리 프로그램*
