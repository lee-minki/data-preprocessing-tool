# 웹 버전 전환 계획 메모

이 파일은 데스크톱 앱을 바로 대체하는 구현 문서가 아니라, 현재 저장소에 있는 `web_app.html` 프로토타입을 어떤 방향으로 발전시킬지 정리한 계획 메모입니다.

## 현재 상태

- `web_app.html`은 단일 HTML 기반 웹 프로토타입입니다.
- 파일 업로드, 필터, 이상값 처리, 시간 처리, 프리셋, CSV 저장 흐름이 들어 있습니다.
- 현재 데스크톱 앱과 1:1 기능 동기화가 끝난 상태는 아닙니다.

## 목표

1. 데스크톱 앱의 핵심 전처리 흐름을 웹에서도 재현
2. 발전운영팀원이 설치 없이 브라우저에서 기본 작업 수행 가능
3. 향후 Validation 데이터 생성 같은 고급 기능도 웹에서 단계적으로 지원

## 우선순위

### 1단계
- CSV 업로드
- 미리보기
- 필터
- 이상값 처리
- 시간 처리
- CSV 저장

### 2단계
- 프리셋 저장/불러오기
- 처리 로그 개선
- 대용량 데이터 대응 방식 검토

### 3단계
- Validation 데이터 생성 웹 지원 검토
- 현재 데스크톱 GUI와 동작/문구 일치화
- 매뉴얼과 웹 화면 연결

## 주의 사항

- 현재 데스크톱 앱에서 제거한 기능은 웹에서도 다시 검토 후 반영해야 합니다.
- 웹 프로토타입은 계획/방향 확인용으로 보고, 운영 반영 전에는 기능 정합성 점검이 필요합니다.
