name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'  # v1.0, v1.1 등 태그 푸시 시 실행
  workflow_dispatch:  # 수동 실행 가능

# 권한 설정 추가
permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get version from tag
      id: get_version
      shell: bash
      run: |
        if [[ "${{ github.ref }}" == refs/tags/* ]]; then
          VERSION=${GITHUB_REF#refs/tags/}
          VERSION=${VERSION#v}
        else
          VERSION=$(python -c "from version import __version__; print(__version__)")
        fi
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Building version: v$VERSION"
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r build_requirements.txt

    - name: Build EXE
      run: |
        pyinstaller --noconfirm --clean DataPreprocessor_windows.spec

    - name: Upload EXE artifact
      uses: actions/upload-artifact@v4
      with:
        name: DataPreprocessor-v${{ steps.get_version.outputs.VERSION }}-Windows
        path: dist/DataPreprocessor_v${{ steps.get_version.outputs.VERSION }}.exe
    
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/DataPreprocessor_v${{ steps.get_version.outputs.VERSION }}.exe
        body: |
          ## 시계열 데이터 전처리 프로그램 v${{ steps.get_version.outputs.VERSION }}
          
          ### 다운로드
          - `DataPreprocessor_v${{ steps.get_version.outputs.VERSION }}.exe` - Windows 실행 파일 (Python 설치 불필요)
          
          ### 주요 기능
          - 📁 Excel/CSV 파일 로드
          - 🔧 다중 조건 필터링 (AND)
          - 📊 이상값 처리 (2σ, 2.5σ, 3σ, IQR)
          - 📈 트렌드 차트 (다중 컬럼, 인터랙티브 커서)
          - 🧪 Validation 데이터 자동 생성 (전처리본 / Validation 포함본 / Validation 전용본)
          - 💾 프리셋 저장/불러오기
          - ⏳ 진행률 표시 (대용량 데이터 지원)
